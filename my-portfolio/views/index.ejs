<%- include('partials/header', { title: 'Portfolio - Ayman Sohail' }) %>
<!-- pulling in my shared header so the <head>, Bootstrap, FA, and navbar stay consistent -->

<!-- Home content -->
<article class="container py-4">
  <!-- I wrapped the page content in a Bootstrap container for nice horizontal padding
       and added py-4 for comfortable vertical spacing -->

  <h2 class="text-center mb-3">Welcome</h2>
  <!-- centered the heading and gave it some space below -->

  <p class="lead text-center mx-auto" style="max-width: 800px;">
    This is my HTML5/CSS3 portfolio built with floats, fluid design, media queries,
    semantic tags, and simple CSS transforms. This portfolio now uses Node.js,
    Express, and EJS templates — combining front-end design with dynamic rendering.
  </p>
  <!-- made the intro a Bootstrap "lead" paragraph, centered it, and limited width
       so it’s easier to read on big screens -->

  <section id="promo" class="mt-4">
    <!-- featured section with a little top margin -->

    <h3 class="h4 text-uppercase text-center mb-3">Featured</h3>
    <!-- smaller, uppercase subheading to match the section vibe -->

    <div class="row g-3 justify-content-center">
      <!-- Bootstrap grid: row with gap between items; center the group -->

      <div class="col-6 col-md-3">
        <div class="card text-center h-100 shadow-sm">
          <div class="card-body">
            <p class="mb-0 fw-semibold">HTML5</p>
          </div>
        </div>
      </div>

      <div class="col-6 col-md-3">
        <div class="card text-center h-100 shadow-sm">
          <div class="card-body">
            <p class="mb-0 fw-semibold">CSS3</p>
          </div>
        </div>
      </div>

      <div class="col-6 col-md-3">
        <div class="card text-center h-100 shadow-sm">
          <div class="card-body">
            <p class="mb-0 fw-semibold">Node.js</p>
          </div>
        </div>
      </div>

      <div class="col-6 col-md-3">
        <div class="card text-center h-100 shadow-sm">
          <div class="card-body">
            <p class="mb-0 fw-semibold">Git</p>
          </div>
        </div>
      </div>
    </div>
    <!-- I replaced my old .box divs with Bootstrap cards inside a responsive grid.
         On phones it shows 2 per row; on md+ it shows 4 in a row. -->
  </section>
</article>

<%- include('partials/footer') %>
<!-- shared footer keeps copyright + social links consistent and closes tags -->
